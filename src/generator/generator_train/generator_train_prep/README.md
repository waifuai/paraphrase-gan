# generator train prep

Prepares data for the training of the paraphrase generator

## Directory structure

The input filepath `paraphrases_selected.tsv` is specified in the `PhraseGeneratorProblem`.

Directory structure
```
data
|-input
  |-paraphrases_selected.tsv
|-output
  |-t2t_data generator
